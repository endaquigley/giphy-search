version: 2

jobs:
  build:
    docker:
      - image: circleci/node:11

    working_directory: ~/giphy-search

    steps:
      - checkout
      - run: sudo npm install -g yarn
      - run: yarn install
      - run: yarn test
      - run: yarn build
